import { CardboxDirective } from './cardbox.directive';

describe('CardboxDirective', () => {
  it('should create an instance', () => {
    const directive = new CardboxDirective();
    expect(directive).toBeTruthy();
  });
});
